# Типографика BS Holding

Единая шкала текста для всех страниц сайта. Размеры, насыщенность и межстрочный
интервал заданы один раз — переменными в `:root` (`src/index.css`). Компоненты
не задают собственные `font-size` в пикселях: они ссылаются на токены.

## Шкала

| Уровень | Токен | Mobile → Desktop | Где применяется |
|---|---|---|---|
| H1 | `--fs-h1` | 32 → 52 px | Главный заголовок страницы, название ЖК в hero. **Один H1 на страницу.** |
| H2 | `--fs-h2` | 26 → 36 px | Заголовки основных секций (`.section-title`, `.easton-h2`, `.about-title`) |
| H3 | `--fs-h3` | 20 → 24 px | Подразделы, карточки ЖК, заголовки поп-апов |
| H4 | `--fs-h4` | 17 → 20 px | Заголовки карточек: преимущества, помещения, планировки, колонки футера |
| Основной текст | `--fs-body` | 15 → 16 px | Описания, абзацы, пункты меню |
| Вторичный текст | `--fs-secondary` | 13 → 14 px | Характеристики, подписи, лейблы форм |
| Микротекст | `--fs-micro` | 12 → 13 px | Категории, метки секций, служебные подписи, согласия |
| Кнопки / CTA | `--fs-btn` | 15 → 16 px | Все кнопки и действия |
| Поля ввода | `--fs-input` | ровно 16 px | `input`, `textarea`, `select` |
| Числовой акцент | `--fs-stat` | 28 → 44 px | Цифры статистики, результат калькулятора |
| Крупный акцент | `--fs-stat-lg` | 40 → 64 px | Выделенная цифра «О компании», код 404 |

Размеры тянутся между mobile и desktop через `clamp()`, поэтому **в media-запросах
размеры больше не переопределяются**. Промежуточные значения (примерно):

| Ширина экрана | H1 | H2 | H3 | H4 | Body |
|---|---|---|---|---|---|
| 375 px | 36 | 27.6 | 20.1 | 17.1 | 15 |
| 768 px | 41.9 | 30.6 | 21.4 | 18.2 | 15.3 |
| 1024 px | 45.8 | 32.5 | 22.3 | 18.9 | 15.6 |
| 1440 px и шире | 52 | 35.6–36 | 23.8–24 | 20 | 16 |

Preferred-часть каждого `clamp()` записана в `rem`, а не в чистых `vw`, — тогда
шкала реагирует на системный размер шрифта пользователя (WCAG 1.4.4).

**Поля ввода — исключение из шкалы:** всегда ровно 16 px. При меньшем размере
iOS Safari масштабирует страницу в момент фокуса на поле.

## Межстрочный интервал

| Токен | Значение | Для чего |
|---|---|---|
| `--lh-h1` | 1.1 | H1 |
| `--lh-h2` | 1.15 | H2 |
| `--lh-h3` | 1.2 | H3 |
| `--lh-h4` | 1.3 | H4 и заголовки карточек в 2–3 строки |
| `--lh-body` | 1.55 | Основной текст |
| `--lh-secondary` | 1.45 | Вторичный текст |
| `--lh-micro` | 1.35 | Микротекст |
| `--lh-ui` | 1.25 | Кнопки, поля, вкладки, пункты меню — высота контрола не должна зависеть от текстового интерлиньяжа |

## Насыщенность

Четыре ступени на весь сайт, промежуточных значений не заводим:

| Токен | Значение | Для чего |
|---|---|---|
| `--fw-body` | 400 | Абзацы и описания |
| `--fw-ui` | 500 | Пункты меню, лейблы форм, подписи в интерфейсе |
| `--fw-emphasis` | 600 | Кнопки, цены, активные состояния, акценты во вторичном тексте |
| `--fw-heading` | 700 | Все заголовки H1–H4 и метки секций |

## Длина строки

Длинные описания ограничены по ширине — иначе строка растягивается на всю секцию
и читается тяжело:

- `--measure: 720px` — основной текст (`.easton-body`, `.about-intro__text`, `.easton-yard__text`, `.calculator__subtitle`);
- `--measure-narrow: 620px` — лид-абзацы под заголовком.

## Как добавлять новые блоки

1. Заголовки размечайте настоящими тегами `h1`–`h4`: базовые правила в
   `src/index.css` уже дают им нужный размер, вес и интерлиньяж. Собственный
   `font-size` при этом не нужен.
2. Если нужен класс — берите токен, а не пиксели:

   ```css
   .my-card__title {
     font-size: var(--fs-h4);
     line-height: var(--lh-h4);
     font-weight: var(--fw-heading);
   }
   ```

3. Не переопределяйте `font-size` внутри `@media` — `clamp()` уже покрывает
   диапазон от мобильного до широкого экрана.
4. Элементы одного смыслового уровня на разных страницах должны использовать
   один и тот же токен. Если для нового блока «не хватает» уровня — почти всегда
   это значит, что блок относится к соседнему уровню, а не что нужен новый размер.

## Проверка

Быстрый способ убедиться, что страница не выпала из шкалы, — выполнить в консоли
браузера:

```js
new Set([...document.querySelectorAll('*')].filter(e => [...e.childNodes].some(n => n.nodeType === 3 && n.textContent.trim())).map(e => getComputedStyle(e).fontSize))
```

На корректной странице в наборе будут только значения шкалы (на 1440 px это
52 / 35.6 / 23.8 / 20 / 16 / 14 / 12.6 px, плюс 40.8 и 61 px для цифр статистики
на «О компании»). Любое постороннее значение — блок с локальным `font-size`.
